<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App2P2023.Views.PageListEmple">

    <ContentPage.ToolbarItems>
        <ToolbarItem x:Name="tooladd" Text="Agregar" Clicked="tooladd_Clicked"/>

        <ToolbarItem x:Name="toollist" Text="Lista" Clicked="toollist_Clicked"/>
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <CollectionView x:Name="list" SelectionMode="Single" SelectionChanged="list_SelectionChanged">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="15">
                    
                </LinearItemsLayout>
            </CollectionView.ItemsLayout>
            
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout>
                        <Label Text="{Binding Nombres}" FontSize="Header"/>
                        <Label Text="{Binding Apellidos}" FontSize="Medium"/>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </ContentPage.Content>
</ContentPage>